<UserControl x:Class="AviatesAirTracker.Views.TelemetryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:AviatesAirTracker.ViewModels"
             xmlns:oxy="http://oxyplot.org/wpf"
             d:DataContext="{d:DesignInstance Type=vm:TelemetryViewModel}"
             Background="{StaticResource Brush_BG_Primary}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <UniformGrid Margin="28,24" Columns="2" Rows="4">

            <Border Style="{StaticResource AviatesCard}" Margin="0,0,10,12" Height="220" Padding="12">
                <Grid>
                    <Border CornerRadius="12" Opacity="0.4">
                        <Border.Background>
                            <RadialGradientBrush Center="0.1,0.2" RadiusX="0.6" RadiusY="0.6">
                                <GradientStop Color="#60A5FA06" Offset="0"/>
                                <GradientStop Color="#60A5FA00" Offset="1"/>
                            </RadialGradientBrush>
                        </Border.Background>
                    </Border>
                    <oxy:PlotView Model="{Binding AltitudePlot}" Background="Transparent"/>
                </Grid>
            </Border>

            <Border Style="{StaticResource AviatesCard}" Margin="0,0,0,12" Height="220" Padding="12">
                <Grid>
                    <Border CornerRadius="12" Opacity="0.4">
                        <Border.Background>
                            <RadialGradientBrush Center="0.1,0.2" RadiusX="0.6" RadiusY="0.6">
                                <GradientStop Color="#06B6D406" Offset="0"/>
                                <GradientStop Color="#06B6D400" Offset="1"/>
                            </RadialGradientBrush>
                        </Border.Background>
                    </Border>
                    <oxy:PlotView Model="{Binding SpeedPlot}" Background="Transparent"/>
                </Grid>
            </Border>

            <Border Style="{StaticResource AviatesCard}" Margin="0,0,10,12" Height="220" Padding="12">
                <Grid>
                    <Border CornerRadius="12" Opacity="0.4">
                        <Border.Background>
                            <RadialGradientBrush Center="0.1,0.2" RadiusX="0.6" RadiusY="0.6">
                                <GradientStop Color="#F9731606" Offset="0"/>
                                <GradientStop Color="#F9731600" Offset="1"/>
                            </RadialGradientBrush>
                        </Border.Background>
                    </Border>
                    <oxy:PlotView Model="{Binding VSPlot}" Background="Transparent"/>
                </Grid>
            </Border>

            <Border Style="{StaticResource AviatesCard}" Margin="0,0,0,12" Height="220" Padding="12">
                <Grid>
                    <Border CornerRadius="12" Opacity="0.4">
                        <Border.Background>
                            <RadialGradientBrush Center="0.1,0.2" RadiusX="0.6" RadiusY="0.6">
                                <GradientStop Color="#22C55E06" Offset="0"/>
                                <GradientStop Color="#22C55E00" Offset="1"/>
                            </RadialGradientBrush>
                        </Border.Background>
                    </Border>
                    <oxy:PlotView Model="{Binding PitchPlot}" Background="Transparent"/>
                </Grid>
            </Border>

            <Border Style="{StaticResource AviatesCard}" Margin="0,0,10,12" Height="220" Padding="12">
                <Grid>
                    <Border CornerRadius="12" Opacity="0.4">
                        <Border.Background>
                            <RadialGradientBrush Center="0.1,0.2" RadiusX="0.6" RadiusY="0.6">
                                <GradientStop Color="#FACC1506" Offset="0"/>
                                <GradientStop Color="#FACC1500" Offset="1"/>
                            </RadialGradientBrush>
                        </Border.Background>
                    </Border>
                    <oxy:PlotView Model="{Binding BankPlot}" Background="Transparent"/>
                </Grid>
            </Border>

            <Border Style="{StaticResource AviatesCard}" Margin="0,0,0,12" Height="220" Padding="12">
                <Grid>
                    <Border CornerRadius="12" Opacity="0.4">
                        <Border.Background>
                            <RadialGradientBrush Center="0.1,0.2" RadiusX="0.6" RadiusY="0.6">
                                <GradientStop Color="#A78BFA06" Offset="0"/>
                                <GradientStop Color="#A78BFA00" Offset="1"/>
                            </RadialGradientBrush>
                        </Border.Background>
                    </Border>
                    <oxy:PlotView Model="{Binding N1Plot}" Background="Transparent"/>
                </Grid>
            </Border>

            <Border Style="{StaticResource AviatesCard}" Margin="0,0,10,0" Height="220" Padding="12">
                <Grid>
                    <Border CornerRadius="12" Opacity="0.4">
                        <Border.Background>
                            <RadialGradientBrush Center="0.1,0.2" RadiusX="0.6" RadiusY="0.6">
                                <GradientStop Color="#F4728006" Offset="0"/>
                                <GradientStop Color="#F4728000" Offset="1"/>
                            </RadialGradientBrush>
                        </Border.Background>
                    </Border>
                    <oxy:PlotView Model="{Binding FuelPlot}" Background="Transparent"/>
                </Grid>
            </Border>

            <!-- 8th chart placeholder -->
            <Border Style="{StaticResource AviatesCard}" Margin="0,0,0,0" Height="220">
                <Grid>
                    <Border CornerRadius="12">
                        <Border.Background>
                            <RadialGradientBrush Center="0.5,0.5" RadiusX="0.4" RadiusY="0.4">
                                <GradientStop Color="#3B82F606" Offset="0"/>
                                <GradientStop Color="#3B82F600" Offset="1"/>
                            </RadialGradientBrush>
                        </Border.Background>
                    </Border>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Additional Telemetry" FontSize="13"
                                   Foreground="{StaticResource Brush_Text_Secondary}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Coming soon" FontSize="11"
                                   Foreground="{StaticResource Brush_Text_Muted}"
                                   HorizontalAlignment="Center" Margin="0,6,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

        </UniformGrid>
    </ScrollViewer>
</UserControl>